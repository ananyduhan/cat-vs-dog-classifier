# cst-vs-dog classifier
## how the project is created
### the data
the raw material for any machine learning model is its training data
for this project i have taken data from kaggle it contains images of dogs and cats but it has some currupt images, some with a different format, some with different size. so the first hurdle is to clean this mess
### libraries used
tenserflow 

### preparing the data
first import the data from kaggle 

### preprocessing 
the data had some images with no lable, some with different format, some with different size and some curroupted. all these problems need to be take care of before they are used to train the actual model

#### cleaning currupt images
using the os module i iterated over all images and deleated those which cannot be opened 

#### changing format 


